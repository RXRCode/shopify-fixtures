import { afterEach, describe, expect, it, vi } from "vitest"; import { ShopifyClient } from "../../src/shopify/client.js"; import type { AuthProvider } from "../../src/auth/provider.js";
const auth:AuthProvider={getAccessToken:async()=>"token"}; afterEach(()=>vi.unstubAllGlobals());
describe("ShopifyClient",()=>{it("sends GraphQL with Admin token",async()=>{const fetchMock=vi.fn(async()=>new Response(JSON.stringify({data:{shop:{name:"Demo"}}}),{status:200,headers:{"Content-Type":"application/json"}}));vi.stubGlobal("fetch",fetchMock);const c=new ShopifyClient("demo.myshopify.com","2026-07",auth);await expect(c.request("query{shop{name}}")).resolves.toEqual({shop:{name:"Demo"}});expect(fetchMock).toHaveBeenCalledOnce();const init=fetchMock.mock.calls[0]![1] as RequestInit;expect((init.headers as Record<string,string>)["X-Shopify-Access-Token"]).toBe("token");});});
